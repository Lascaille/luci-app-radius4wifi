prefix = /usr
exec_prefix = ${prefix}
sysconfdir = /etc
localstatedir = /var
name = radius4wifi

raddbdir = ${sysconfdir}/radius4wifi
confdir = ${raddbdir}
certdir = ${raddbdir}/certs

log {
    destination = stdout
    auth = yes
}

modules {
    $INCLUDE mods-enabled/
}

$INCLUDE clients.conf
$INCLUDE sites-enabled/