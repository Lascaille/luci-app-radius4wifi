eap {
    default_eap_type = tls

    tls-config conf_tls {
        private_key_file = ${raddbdir}/pki/server.key
        certificate_file = ${raddbdir}/pki/server.pem
        ca_file = ${raddbdir}/pki/ca.pem
        tls_min_version = "1.2"
    }

    tls {
        tls = conf_tls
    }
}
